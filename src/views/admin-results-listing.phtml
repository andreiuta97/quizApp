<?php
require_once('partials/head.phtml');
?>
    <title>Admin - Results listing</title>
</head>

<body>
    <?php
        require_once('partials/header.phtml');
    ?>

    <!--Breadcrumbs-->
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb px-3 py-0 mb-3">
            <li class="breadcrumb-item" ><a href="/admin/dashboard">Dashboard</a></li>
            <li class="breadcrumb-item active" aria-current="page">Results listing</li>
        </ol>
    </nav>

    <div class="container">
        <!--Page title block-->
        <div class="text-center">
            <h1>Results listing</h1>
        </div>

        <!--Quiz listing table-->
        <div class="table-responsive my-4">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <!--Sorting arrow are displayed using the 'asc','dsc' classes-->
                    <th scope="col">ID</th>
                    <th scope="col">User</th>
                    <th scope="col">Quiz</th>
                    <!--Sorting arrow are displayed using the 'asc','dsc' classes-->
                    <th scope="col">Score</th>
                    <th scope="col">Actions</th>
                </tr>
                </thead>
                <tbody>
                <?php foreach ($data as $key => $result): ?>
                <tr>
                    <th scope="row"><?php echo  $result['quiz_instance_id']; ?></th>
                    <td><?php echo $result['name'] ?> </td>
                    <td><?php echo $result['quiz_template_id'] ?></td>
                    <td>We don't believe that numbers represent the value of people.</td>
                    <td class="text-center">
                        <a class="btn btn-primary py-1" href="/result/<?php echo $result['user_id']; ?>/<?php echo  $result['quiz_instance_id']; ?>">View</a>
                    </td>
                </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>